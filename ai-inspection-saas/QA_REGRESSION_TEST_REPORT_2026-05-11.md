# AI 视频巡检 SaaS MVP 回归测试报告

回归日期：2026-05-11  
回归地址：http://127.0.0.1:8090/  
回归账号：operator01 / 123456，admin / 123456  
基线报告：`QA_FUNCTIONAL_TEST_REPORT.md`

## 1. 回归结论

研发与产品本轮更新后，上轮报告中的 P0/P1 主要缺陷均已完成有效修复并通过回归。

本轮接口回归执行 44 个验证点，44 个通过，0 个失败。页面回归覆盖 operator 权限菜单、admin 多项目切换和空项目展示，均通过。

## 2. 上轮缺陷回归状态

| 原缺陷 | 模块 | 上轮问题 | 本轮结果 |
| --- | --- | --- | --- |
| BUG-001 | 权限 | `operator01` 可查看审计日志 | 已修复，返回 403 `audit_forbidden` |
| BUG-002 | 权限/点位 | `operator01` 可保存点位 | 已修复，返回 403 `map_config_forbidden` |
| BUG-003 | 项目上下文 | admin 切到 `mall_002` 后页面残留 `mall_001` 数据 | 已修复，页面标题、指标、AI 状态、地图点位均刷新为 `虹桥商业裙楼` 空态 |
| BUG-004 | 大屏/项目 | `mall_002` 默认 L1 点位返回 404 | 已修复，返回 200，`points=[]` |
| BUG-005 | AI 接入 | `/ai-events/source-events` 未实现 | 已修复，合法事件返回 202，可生成 SourceEvent/告警 |
| BUG-006 | 实时订阅 | 实时 token/SSE 缺失 | 已修复，token 接口和 SSE connected 事件可用，token 绑定项目 |
| BUG-007 | 规则配置 | 规则只有列表，无 CRUD | 已修复，新增、编辑、启停、复制、删除均通过 |
| BUG-008 | 证据/附件 | 下载令牌、删除、附件、导出缺失 | 已修复，下载 token、软删除、附件记录、导出任务接口均通过 |
| RISK-001 | 摄像头安全 | 摄像头列表返回 `streamAddressRef` | 已修复，响应不再返回源流引用字段 |

## 3. 接口回归摘要

| 模块 | 验证点 | 结果 |
| --- | --- | --- |
| 登录与权限 | 登录成功/失败、项目成员隔离、operator 禁止审计/点位/地图/规则/证据删除 | 通过 |
| 摄像头 | 列表、楼层筛选、状态筛选、AI 能力、源流字段脱敏 | 通过 |
| 点位与地图 | 点位读取、版本冲突、坐标越界、恶意 SVG 拦截 | 通过 |
| 项目切换 | `mall_002` summary、楼层基础数据、L1 空点位 | 通过 |
| 媒体会话 | 可预览创建会话、断流返回 failureReason、释放会话 | 通过 |
| 告警闭环 | 列表、待确认不可指派、误报原因必填 | 通过 |
| 规则配置 | 新增、编辑、启停、复制、删除 | 通过 |
| AI 接入 | 正确签名、幂等、低置信度丢弃、映射失败、错误签名、nonce 重放 | 通过 |
| 实时订阅 | token 创建、SSE connected、token 项目隔离 | 通过 |
| 证据/附件/导出 | 下载 token、软删除、附件记录、告警导出任务 | 通过 |

## 4. 页面回归摘要

| 场景 | 步骤 | 预期 | 结果 |
| --- | --- | --- | --- |
| operator 菜单权限 | `operator01` 登录后打开管理入口 | 只显示摄像头管理、告警查询 | 通过 |
| admin 多项目 | `admin` 登录后项目选择器展示双项目 | 可选择大悦城示例项目、虹桥商业裙楼 | 通过 |
| admin 切换空项目 | 从 `mall_001` 切到 `mall_002` | 页面刷新为空项目数据，不串项目 | 通过 |
| `mall_002` 大屏空态 | 查看 `mall_002` 指挥中心 | 摄像头在线率 0%、今日告警 0、暂无 AI 巡检项、当前楼层暂无点位、暂无可轮巡视频 | 通过 |
| 浏览器错误日志 | 切换项目后读取 console error | 无前端运行错误 | 通过 |

## 5. 本轮执行影响

本轮回归为了验证真实链路，产生了以下测试数据或审计记录：

- 创建并释放媒体播放会话。
- 创建、编辑、复制并删除临时规则。
- 推送多条 AI 测试事件，其中一条合法事件生成了测试告警。
- 创建实时订阅 token 并连接 SSE。
- 创建证据下载 token，执行一次证据软删除接口。
- 上传一次告警附件记录，创建一次告警导出任务。

## 6. 剩余风险

虽然本轮回归通过，但从 PRD 完整度看，以下点建议后续继续强化：

- SSE 当前验证到 connected 事件，尚未验证真实 `alarm.created/alarm.updated/device.statusChanged` 推送。
- 附件上传目前是动作记录，未做真实文件落库、预览、大小/病毒扫描等完整附件链路。
- 证据软删除当前返回成功并写审计，但未看到独立 Evidence 表的删除状态持久化。
- 导出任务当前直接 `completed`，尚不是异步任务队列模型。
- AI 冷却去重当前按未关闭告警过滤，尚未完整验证 `cooldownSeconds + ROI + 时间窗口` 的精确策略。

## 7. 回归结论建议

本轮修复可判定为通过回归，建议进入下一轮功能增强或提测前自动化固化阶段。建议把本轮 44 个接口验证点纳入 smoke/regression 脚本，并补充真实 SSE 推送、附件文件、Evidence 状态、异步导出和精确冷却策略的专项测试。
